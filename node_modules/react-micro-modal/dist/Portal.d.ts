import React from 'react';
export declare type ModalPortalProps = {
    children: React.ReactNode;
    parent?: () => HTMLElement;
};
export declare const ModalPortal: ({ parent, children }: ModalPortalProps) => React.ReactPortal;
